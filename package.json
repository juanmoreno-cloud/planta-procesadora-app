{
  "name": "optiflow",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de gestión para planta procesadora de alimentos (charcutería/embutidos): catálogos, producción, trazabilidad, stock y dashboards.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Aún no configurado. Se habilitará al crear apps/web y apps/api.\"",
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
